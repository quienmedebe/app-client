import styled from 'styled-components/native';
import {Poppins} from '../../../theme/fonts';
import {darkText} from '../../../theme/colors';

export default styled.Text`
  font-family: ${Poppins.Regular};
  font-size: 16px;
  color: ${darkText};
`;
